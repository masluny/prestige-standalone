fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
owlready2>=0.46         # optional: reasoners, RDF/XML export, SPARQL

# --- Standalone-only extras (used by standalone.py + the PyInstaller spec) ---
# Install these if you want a native desktop window or want to build .app/.exe:
#   pip install pywebview>=5 pyinstaller>=6
pywebview>=5.0
pyinstaller>=6.5
